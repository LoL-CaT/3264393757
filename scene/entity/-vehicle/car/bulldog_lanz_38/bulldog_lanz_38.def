{entity
	(include "/properties/car.ext")
	(include "/properties/shadow/tank.inc"  scale(1.0))
	(include "/properties/selection/vehicle.inc"  scale(1.1))
	(include "/properties/action_target/repair.inc")	
	
	
	{props "vehicle" "car" "wheeled" "truck" "unarmed" "texmod_w"}
	{able "emit_on_attack"}
	("armor_medium" args 1.0)
	
		("crew_inside")
		
	("crew_human" place(driver)		bone(driver)	door(emit1) animation(driver))
	("board_animation"
		place(driver)
		board_begin(board_big-midcar_driver)
		board_end(driver_car_big)
;		base(doorleft)
	)
	("wheel" args "wheelright1")
	("wheel" args "wheelleft1")
	("wheel" args "wheelright2")
	("wheel" args "wheelleft2")

	{mass 2000}
	{Chassis
		{Locomotion ("locomotion")
			{MaxSpeed	26.5}
			{StartTime	3}
			{maxSpeedAtMaxTurn 12.25}
			{BrakeTime	1}
			{StopTime	1}
			{TurnRadius	5}
			{Gears		0.1 0.2 0.5 1.0}
			{RearGears	0.5}
			{SteerOn	20}
			{SteerOff	40}
			{SplineRadius 4}
		}
		("fuelbag"
			volume(75)
			rate(50)
			remain(75)
		)		
		("car_trace" fx("tracks_sm") step(0.3) wheell("wlf") wheelr("wrf") bonel("FXtraceL2") boner("FXtraceR2"))
		{MaxTrailerMass 1375}		
	}
	("chassis_add" speed(26.5))	
	{extension "bulldog_lanz_38.mdl"}
    ("animation_resume")
}
