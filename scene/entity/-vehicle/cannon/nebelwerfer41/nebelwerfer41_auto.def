{entity
	(include "/properties/cannon.ext")
	(include "/properties/shadow/tank.inc"  scale(0.6))
	{Extension "nebelwerfer41.mdl"}
	(include "/properties/selection/cannon.inc"  scale(0.7))
	{props "vehicle" "cannon" "cannon_small" "nebelwerfer" "wheeled" "round_sandbag2" "vision_lev05_can" "reactive_artyllery"}
;	{able "emit_on_attack"}
	{targetSelector "cannon_rocket"}
	{collider "cannon_small"}
	
	{volume "body*"
        {tags "body"}
		{component "body"}
	}
	("v_gun")
	("armor_light_hp" hp(250))
	("round_sandbag2")
	("round_dot2")

	("wheel" args "wheelright")
	("wheel" args "wheelleft")

    ("crew_wheeled_gun")
	("crew_2_supporter")

	{boarder
		{anm "gunner"
			{forward	{end "pose_nebel42_gunlayer" 10}}
		}
		{anm "commander"
			{forward	{end "pose_pak40_charger" 10}}
		}
		{anm "driver1"
			{forward {end "cannon_pak40_pusher_left" 10}}
		}
		{anm "driver2"
			{forward {end "cannon_pak40_pusher_right" 10}}
		}
		{anm "seat1"
			{forward {end "pose_seat_cannon1_left" 10}}
		}
		{anm "seat2"
			{forward {end "pose_seat_cannon1_right" 10}}
		}
	}
	{extender "inventory"
		{box
			{item "ammo 150mm_rocket" 6}
			{item "sandbag_kit2"}
		}
	}
	{Weaponry
		{place "gun"
			{weapon "nebel41" filled}
			{Barrels 6}
			{StationaryFiring}
			("abm_field_gun")			
		}
	}
	{mass 540}
	{extender "cannon"
		{move {workers clear} {obstacle "close"}}
		{attack {workers clear} {obstacle "open"}}
	}
	{autooperatable 1}
	{placer
		{crew 0}
		{place "gunner" remove}
		{place "commander" remove}
	}

	{Chassis "track"
		("car_trace" fx("tracewheel05") step(0.07) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("loco_big")
	}
	{brain
		{properties
			{fire_mode hold}
		}
	}
	{bone "gun"
		{limits -65 5}
	}
	{extension "sensor.ext"}
	{extension "camouflage.ext"}	
}