;SDL
{textures
	{alpha alpha_channel 		; bitmaps with alpha blend textures (used in fx particles) - битмапы альфа-блендом для спецэффектов
		(include "alpha.inc")
    }
	{light 						; bitmaps with blend light (add) textures (used in fx particles) - битмапы с лайт-прозрачностью для спецэффектов
		(include "light.inc")
	}
	{grass alpha_channel uncompressed ; grass textures - текстуры травы
		(include "grass.inc")
	}
} 
