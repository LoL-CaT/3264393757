{selector
	{unit "human" "fg"};хуман с fg weapon
	{group
		{select}		
		{ignore {class airborne}}	
		{sort {class tank}}
		{sort {class light_tank}}		
		{sort {class car}}		
		{sort {class armoredcar}}
		{sort {class house}}
		{sort {class cannon}}
		{sort {class cannon_howitzer}}
		{sort {class cannon_tower}}		
		{sort {class trench}}
		{sort {class human} time_to_aim}		
		{sort distance}
    }
}